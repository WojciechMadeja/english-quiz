import { Wrapper } from "./ArrowLeft.styles";
import arrow from '../../../assets/arrowleft.svg'

function ArrowLeft(params) {
    return(
        <Wrapper src={ arrow }></Wrapper>
    );
}

export default ArrowLeft;